<header>
    <nav class="navbar shadow-sm py-3 <%= navbarClass || 'bg-church' %>" style="background-color: #457507">
        <div class="container-fluid px-4 d-flex justify-content-between align-items-center">
            <% if (headerType === 'login') { %>
                <a href="index.html" class="d-flex align-items-center text-white text-decoration-none">
                    <img class="rounded-circle" src="/images/logo.jpg" width="45" height="45" class="me-2" alt="Logo">
                    <div>
                        <span class="fw-bold fs-4 mx-2 text-white">Archdiocese of Tuguegarao</span>
                        <h6 class="mx-2" id="subtext">Pastoral Office</h6>
                    </div>
                </a>
            <% } else if (headerType === 'user' || headerType === 'archdiocese') { %>
                <a href="#" class="d-flex align-items-center text-white text-decoration-none">
                    <img class="rounded-circle" src="/images/logo.jpg" width="45" height="45" class="me-2" alt="Logo">
                    <div>
                        <span class="fw-bold fs-4 mx-2 text-white">Archdiocese of Tuguegarao</span>
                        <h6 class="mx-2" id="subtext">Pastoral Office</h6>
                    </div>
                </a>
                <div class="dropdown">
                    <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" id="profileDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        <span id="name" class="d-none d-sm-inline"><%= username || 'Username' %></span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="profileDropdown">
                        <li><a class="dropdown-item text-danger" id="signoutBtn" href="login.html"><i class="bi bi-box-arrow-right me-2"></i> Sign out</a></li>
                    </ul>
                </div>
            <% } else { %>
                <a href="#" class="d-flex align-items-center text-white text-decoration-none">
                    <img class="rounded-circle" src="/images/logo.jpg" width="45" height="45" class="me-2" alt="Logo">
                    <div>
                        <span class="fw-bold fs-4 mx-2 text-white">Archdiocese of Tuguegarao</span>
                        <h6 class="mx-2" id="subtext">Pastoral Office</h6>
                    </div>
                </a>
                <% if (headerType === 'default') { %>
                    <a href="/login" class="btn btn-light fw-semibold px-4 rounded-pill">
                        <i class="bi bi-person-circle"></i> Login
                    </a>
                <% } %>
            <% } %>
        </div>
    </nav>
</header>